package com.hbvibe.banner.entity;

public enum BannerPosition {
    HOME_SLIDER,      // Banner trượt to nhất ở trang chủ
    HOME_SUB_GRID,    // 4 banner nhỏ ở dưới slider trang chủ
    AUTH_BG,          // Ảnh nền cho trang Đăng nhập / Đăng ký
    CATEGORY_HEADER,  // Banner trên cùng của trang danh mục sản phẩm
    PROMO_POPUP       // Banner quảng cáo nhảy lên (Popup)
}
