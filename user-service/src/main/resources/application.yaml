server:
  port: 8081
  servlet:
    context-path: /user

spring:
  application:
    name: user-service
  datasource:
    url: "jdbc:postgresql://localhost:5434/user_db"
    driver-class-name: "org.postgresql.Driver"
    username: root
    password: rootpassword
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8080/realms/devteria
idp:
  url: http://localhost:8080
  client-id: HBvibe_web_backend
  client-secret: LoyUWYLqwJ2gt91JdIYd2ZiSkARTurdU

